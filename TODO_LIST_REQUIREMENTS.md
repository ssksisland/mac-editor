# Todo List 功能需求文档

## 1. 文档信息

- 功能名称：Todo List
- 文档版本：v1.0
- 产品范围：Mac Editor 桌面端
- 当前阶段：正式实现

## 2. 功能目标

在 Mac Editor 中增加一个独立的 Todo List 页面，方便用户在编辑文件的同时记录和管理待办事项。

Todo List 页面通过标签页栏最左侧的固定 Todo 标签进入。该标签用于页面切换，但不属于普通文件标签页，不参与文件打开、保存、重命名和关闭确认流程。

## 3. 固定标签与页面切换

### 3.1 固定 Todo 标签

- 在标签页栏最左侧增加一个固定 Todo 标签。
- Todo 标签始终显示，不因打开或关闭文件而消失。
- Todo 标签固定在所有普通文件标签之前，不能通过拖拽改变位置。
- 普通文件标签不能被拖动到 Todo 标签左侧。
- Todo 标签不可关闭、不可重命名，不显示文件修改状态圆点。
- Todo 标签需要有明确的待办事项图标和 `Todo` 文案。
- 点击 Todo 标签进入 Todo List 页面。
- Todo List 页面打开时，Todo 标签使用与活跃文件标签一致的选中反馈，同时保留独立图标以便识别。

### 3.2 与普通标签页的区别

- Todo 标签是应用级固定页面入口，不对应磁盘文件，也不写入普通文件 tab 数据。
- 进入 Todo List 页面后，保留当前所有文件 tab 及其编辑状态。
- 点击任意普通文件标签后，离开 Todo List 页面并显示对应文件编辑器。
- 重复点击已经选中的 Todo 标签不切换页面，也不触发其他操作。
- 返回编辑区域时，恢复进入 Todo List 前最后激活的文件 tab、光标和滚动位置。
- Todo List 页面不显示文件编辑器的状态栏信息，例如语言、编码和光标行列。
- 当没有打开任何普通文件时，标签栏仍显示 Todo 标签；Todo 页面未选中时可以继续显示现有欢迎页。

## 4. 页面结构

Todo List 页面包含以下区域：

1. 页面标题：`Todo List`。
2. 新建待办区域：用于输入并创建待办事项。
3. 待办列表：展示尚未完成的事项。
4. 已完成列表：展示已经完成的事项。

待办列表和已完成列表需要有明显的视觉分区。已完成列表可以折叠和展开，默认展开。

## 5. 待办事项功能

### 5.1 新建

- 用户可以在新建待办区域输入事项内容。
- 按 Enter 或点击新增按钮后创建事项。
- 内容去除首尾空白后不能为空；空内容不创建。
- 创建成功后清空输入框，并将新事项加入待办列表。
- 每个事项在创建时自动记录创建时间。
- 创建时间使用毫秒时间戳记录，展示格式为 `YYYY-MM-DD HH:mm`。

### 5.2 展示

每条未完成事项包含：

- 左侧空心圆形完成按钮。
- 待办内容。
- 靠近列表项右边框的创建时间，使用浅灰色备注样式。
- 编辑操作。
- 删除操作。

待办内容过长时应自动换行，不应撑破页面布局。

### 5.3 编辑

- 用户点击编辑操作或双击内容后进入行内编辑状态。
- 编辑状态下可以修改事项文本。
- 按 Enter 保存，按 Escape 取消。
- 点击该事项外部时保存修改。
- 修改文本不改变原始创建时间。
- 修改后的内容不能为空；为空时不保存，并保留编辑状态或恢复原内容。

### 5.4 完成

- 用户点击事项左侧的空心圆点，将该事项标记为完成。
- 点击后圆点先显示完成状态，再执行渐进式退出动画。
- 建议动画为淡出并向右轻微移动，高度同步收起，持续 `250-350ms`。
- 动画结束后，该事项从待办列表移除，并进入已完成列表。
- 完成时自动记录完成时间。
- 动画进行期间禁用该事项的重复点击、编辑和删除操作。

### 5.5 删除

- 用户可以删除未完成事项或已完成事项。
- 删除表示永久移除，不进入已完成列表。
- 删除前需要二次确认，避免误操作。
- 删除成功后使用与完成操作一致或更短的渐隐收起动画。

## 6. 已完成列表

### 6.1 展示内容

每条已完成事项包含：

- 完成状态圆点或勾选图标。
- 待办内容，使用弱化样式，可以增加删除线。
- 浅灰色创建时间备注：`创建于 YYYY-MM-DD HH:mm`。
- 浅灰色完成时间备注：`完成于 YYYY-MM-DD HH:mm`。
- 永久删除操作。

时间信息在列表项右侧对齐；空间不足时可以换到下一行，但仍需保持右对齐和备注视觉层级。

### 6.2 排序

- 已完成列表按照事项的创建时间倒序排列，最新创建的事项在最上方。
- 创建时间相同时，按照完成时间倒序排列。
- 排序只影响展示顺序，不修改时间记录。

## 7. 空状态

- 没有未完成事项时，显示简洁的空状态提示，例如“暂无待办事项”。
- 没有已完成事项时，在已完成区域显示“暂无已完成事项”。
- 空状态不应占用过多页面空间。

## 8. 数据与持久化

- Todo 数据保存在本机，不依赖网络。
- 应用关闭并重新启动后，待办和已完成事项仍然存在。
- Todo 数据不跟随当前文件或项目切换，是整个应用共享的数据。
- 保存失败时不能静默丢失用户操作，应保留当前界面数据并给出错误提示。
- 正式数据保存在 Tauri 应用数据目录的 `todos.json`，不依赖 WebView 缓存。
- 每次覆盖主文件前保留 `todos.json.bak`；主文件损坏时自动尝试读取备份。
- 写入通过同目录临时文件同步并替换，降低应用异常退出时文件损坏的风险。
- 数据文件包含 schema 版本号，用于后续版本兼容和迁移。
- 首次升级正式版时，如果磁盘数据为空，自动迁移 Demo 使用的 `localStorage` 数据；仅在写入成功后清理旧数据。

每条 Todo 至少包含以下字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 唯一标识 |
| `content` | string | 事项内容 |
| `status` | `pending \| completed` | 当前状态 |
| `createdAt` | number | 创建时间戳 |
| `completedAt` | number 或 null | 完成时间戳 |

## 9. 视觉与交互要求

- 整体样式与现有 Mac Editor 的浅色界面保持一致。
- Todo List 页面需要通过页面标题、内容布局和固定 Todo 标签的选中状态，与普通编辑器明显区分。
- 空心圆点应具有足够大的点击区域，视觉直径建议 `14-18px`，实际点击区域不小于 `28px`。
- 时间信息使用浅灰色、小字号备注样式，但需要满足基本可读性。
- 编辑、删除等操作默认弱化，可在当前行悬浮或聚焦时突出显示。
- 所有按钮需要提供 hover、active 和 keyboard focus 状态。

## 10. 键盘与可访问性

- 新建输入框支持 Enter 创建。
- 编辑状态支持 Enter 保存、Escape 取消。
- 完成按钮、编辑按钮、删除按钮均可通过 Tab 键聚焦并通过 Enter/Space 操作。
- 完成按钮应提供可访问名称，例如“完成待办：事项内容”。
- 动画应遵守系统的“减少动态效果”设置；开启该设置时缩短或取消位移动画。

## 11. 验收标准

1. 标签页栏最左侧始终存在固定 Todo 标签，点击后进入 Todo List 页面，且不新增普通文件 tab。
2. Todo 页面与普通编辑器视觉上可以明确区分。
3. 用户可以新建、编辑、完成和永久删除事项。
4. 每条事项创建时自动记录创建时间，完成时自动记录完成时间。
5. 点击空心圆点后，事项通过渐进动画从待办列表移入已完成列表。
6. 已完成列表同时展示创建时间和完成时间，并按创建时间倒序排列。
7. 完成操作不会被当作永久删除，已完成事项可以继续查看。
8. 永久删除未完成或已完成事项前会要求确认。
9. 应用重启后 Todo 数据完整保留。
10. 切换 Todo 页面不会丢失普通文件 tab 的内容、修改状态、光标或滚动位置。
11. 连续快速点击完成按钮不会产生重复事项或状态错误。
12. 空内容无法创建或保存。
13. Todo 标签不可关闭、重命名或拖动，普通文件标签也不能移动到它的左侧。

## 12. 暂不包含

- 截止时间、提醒和系统通知。
- 优先级、标签、分组和搜索。
- 拖拽排序。
- 已完成事项恢复为未完成。
- 云同步和多设备同步。
- Todo 数据导入和导出。

## 13. 已确认规则

正式版采用以下规则：

- 已完成列表按创建时间倒序排列，而不是正序排列。
- 永久删除需要二次确认。
- Todo 数据为应用级共享，并使用本地持久化。
- 第一版不支持将已完成事项恢复为未完成。
