# Changelog

All notable changes to mac-editor will be documented in this file.

## [v0.4.0] — 2026-06-12

### Added
- 行标记（书签）— 点击行号在该行号右侧生成蓝色圆点标记整行，再次点击取消，类 Notepad++
- 标签页重命名同步磁盘文件 — 双击 tab 改名时，已保存文件会在原目录重命名实际文件（拒绝覆盖同名文件）
- Cmd 拖动列选区 — 按住 Cmd（Windows 为 Ctrl）拖动鼠标生成矩形多光标列选区
- 多编码保存 — 按文件原始检测编码（GBK、Shift-JIS 等）回写，不再强制转成 UTF-8

### Changed
- gutter 布局调整：分隔竖线移至行标记列右侧，折叠列位于竖线右侧
- 状态栏编码显示改为读取当前文件实际检测编码，不再硬编码 UTF-8
- 文件读写统一走 Rust 命令（read_file_cmd / save_file_cmd），打通编码检测与回写链路
- 原生与 HTML5 文件拖放统一接入编码检测

### Fixed
- 搜索：正则模式下零宽匹配（如 `.*`、`^`、`\b`）导致界面死锁
- 保存：非 UTF-8 文件被静默转为 UTF-8 导致内容损坏
- 文件语言识别：无扩展名特殊文件名（Dockerfile、README、LICENSE）检测失效
- 标签页重命名不生效（WebKit 下可拖拽父元素吞掉输入焦点）且会清空文件路径
- 从外部拖入文件时内容被写进当前标签页（CodeMirror 默认拖放行为）
- 多行选区中间行行首多出一段空白（content 左 padding 导致）
- 替换全部时 `$` 替换模式与单个替换行为不一致
- 不可见字符在大文件中遍历整篇文档的性能问题，改为仅渲染视口内
- 快捷键在 Caps Lock 开启时失效（字母改用小写比较）
- Cmd+Tab 在无标签页时越界报错
- Markdown 预览异步图片加载竞态、关闭确认对话框 setTimeout 与窗口关闭监听器未清理
- 安全：图片预览读取限制为已知图片扩展名，拒绝任意本地文件读取

## [v0.3.0] — 2026-05-29

### Added
- Markdown 实时预览 — 编辑 .md 文件时点击工具栏眼睛按钮，右侧显示渲染后的预览面板（左右分屏）
- 本地图片支持 — 预览中绝对路径和相对路径图片通过 Rust 读取为 base64 data URL 显示
- GitHub 风格 Markdown 渲染样式（标题、代码块、表格、引用块、任务列表等）

## [v0.2.0] — 2026-05-22

### Added
- 按行去重功能（⌘⇧D），有选区去重选中行，无选区全文去重
- Tab 拖拽排序，蓝色插入指示条，Chrome 风格标签视觉（活跃 tab 浮起、非活跃透明融合）
- 关闭保存确认 — 关闭窗口时逐个询问每个未保存文件，保存/不保存/取消三按钮（Excel 风格）
- 单 tab 关闭确认 — 点击 × 或 Cmd+W 关闭已修改文件时弹出保存询问
- 新建 tab 顺序命名："未命名"→"未命名1"→"未命名2"...
- 工具栏：按行去重按钮、列选择按钮

### Changed
- 工具栏去掉字号下拉框和语言图标，语言名称统一英文
- Tab 修改标记圆点缩小至 7px

### Fixed
- 保存新文件时取消另存为对话框后，tab 不应被关闭
- Cmd+W 关闭已修改 tab 时绕过保存确认
- 批量关闭过程中仍可触发单个 tab 关闭导致对话框叠加
- CRLF 换行符在不可见字符视图中检测错误

## [v0.1.0] — 2026-05-19

### Added
- 初始版本：Tauri 2 + React 19 + CodeMirror 6 桌面编辑器
- 多标签页编辑，30+ 语言语法高亮
- 文件打开/保存/另存为，编码自动检测（chardetng）
- 搜索替换面板（正则、大小写、转义序列、标记所有）
- 不可见字符显示切换
- 跳转到指定行
- 多光标编辑（Cmd+Click、列选择）
- 最近文件列表（localStorage 持久化）
- 文件拖放打开
- 快捷键支持（新建/打开/保存/关闭/搜索/字体缩放/切换 tab）
